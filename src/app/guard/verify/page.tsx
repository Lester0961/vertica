import { PagePlaceholder } from "@/components/design-system/PagePlaceholder";

export default function GuardVerifyPage() {
  return (
    <PagePlaceholder
      title="Verify gate pass"
      description="Scan or enter a gate-pass code to verify visitor access. Full-screen camera verification arrives with the gate-pass module."
      phase="Phase 11 (Gate passes)"
    />
  );
}
