import { PagePlaceholder } from "@/components/design-system/PagePlaceholder";

export default function AdminDashboardPage() {
  return (
    <PagePlaceholder
      title="Administration dashboard"
      description="Operational priorities, occupancy, collections, and funnel metrics will appear here."
      phase="Phase 12 (Reports and MIS)"
    />
  );
}
