import { PagePlaceholder } from "@/components/design-system/PagePlaceholder";

export default function TenantDashboardPage() {
  return (
    <PagePlaceholder
      title="Resident dashboard"
      description="Your unit, active lease, next bill, balance, maintenance status, and gate passes will appear here."
      phase="Phase 8 (Tenants and leases)"
    />
  );
}
